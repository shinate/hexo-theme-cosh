<% if (post.tags && post.tags.length){ %>
<div class="article-tag-list flex-column">
    <%- list_tags(post.tags, {
        show_count: false,
        class: 'article-tag',
        style: 'none',
        separator: ''
    }) %>
</div>
<% } %>