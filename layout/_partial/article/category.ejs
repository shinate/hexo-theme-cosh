<% if (post.categories && post.categories.length){ %>
<div class="article-category-list flex-column">
    <%- list_categories(post.categories, {
        show_count: false,
        show_current: true,
        class: 'article-category',
        style: 'none',
        separator: '>'
    }) %>
</div>
<% } %>